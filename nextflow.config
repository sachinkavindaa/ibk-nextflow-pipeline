params {
    input  = "samplesheet.csv"
    outdir = "results"
}

process {
    cpus = 6
    memory = "32 GB"
    time = "168h"
}

profiles {
    hcc {
        process.executor = "slurm"
        process.queue = "guest,batch"
        process.module = "bbmap/39.06"
    }
}
